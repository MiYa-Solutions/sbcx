<%= simple_form_for job.becomes(ServiceCall), html: { class: 'green-button' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'deposited' %>
    <%= f.submit job.class.human_billing_status_event_name(:deposited).titleize,
                 id:    'deposited_service_call_btn',
                 class: 'btn btn-large btn-success',
                 title: I18n.t('service_call.forms.billing_status.deposited.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

