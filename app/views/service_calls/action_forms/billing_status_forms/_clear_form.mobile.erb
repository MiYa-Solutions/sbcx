<%= simple_form_for job.becomes(ServiceCall), html: { class: 'green-button' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'clear' %>
    <%= f.submit job.class.human_billing_status_event_name(:clear).titleize,
                 id:    'clear_service_call_btn',
                 class: "ui-btn-up-b ui-btn-hidden",
                 "data-theme" => "b",
                 title: I18n.t('service_call.forms.billing_status.clear.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

