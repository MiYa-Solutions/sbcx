<%= simple_form_for job.becomes(ServiceCall), html: { class: 'blue-button' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'subcon_invoiced' %>
    <%= f.submit job.class.human_billing_status_event_name(:subcon_invoiced).titleize,
                 id:    'subcon_invoiced_service_call_btn',
                 class: 'btn btn-large btn-primary',
                 title: I18n.t('service_call.forms.billing_status.subcon_invoiced.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

