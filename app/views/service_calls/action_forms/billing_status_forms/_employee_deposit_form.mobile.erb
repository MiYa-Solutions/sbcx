<%= simple_form_for job.becomes(ServiceCall), html: { class: 'blue-button' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'employee_deposit' %>
    <%= f.submit job.class.human_billing_status_event_name(:employee_deposit).titleize,
                 id:    'employee_deposit_service_call_btn',
                 class: "ui-btn-up-b ui-btn-hidden",
                 "data-theme" => "b",
                 title: I18n.t('service_call.forms.billing_status.deposit_to_prov.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

