<%= simple_form_for job.becomes(ServiceCall), html: { class: 'blue-button' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'provider_invoiced' %>
    <%= f.submit job.class.human_billing_status_event_name(:provider_invoiced).titleize,
                 id:    'provider_invoiced_service_call_btn',
                 class: "ui-btn-up-b ui-btn-hidden",
                 "data-theme" => "b",
                 title: I18n.t('service_call.forms.billing_status.provider_invoiced.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

