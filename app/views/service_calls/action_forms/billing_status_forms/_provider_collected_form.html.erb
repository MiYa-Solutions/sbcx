<%= simple_form_for job.becomes(ServiceCall), html: { class: 'blue-blue' } do |f| %>
    <%= hidden_field_tag 'service_call[billing_status_event]', 'provider_collected' %>
    <%= hidden_field_tag 'service_call[collector_id]', job.provider.id %>
    <%= hidden_field_tag 'service_call[collector_type]', 'Organization' %>
    <%= f.input :payment_type, collection: payment_types, input_html: { id: 'provider_payment_type' } %>
    <%= f.submit job.class.human_billing_status_event_name(:provider_collected).titleize,
                 id:    'provider_collected_service_call_btn',
                 class: 'btn btn-large btn-primary',
                 title: I18n.t('service_call.forms.billing_status.provider_collected.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

