
<%= simple_form_for job.becomes(ServiceCall), html: { class: '' } do |f| %>
    <%= hidden_field_tag 'service_call[work_status_event]', 'reset' %>
    <%= f.submit job.class.human_work_status_event_name(:reset).titleize,
                 id:    'reset_service_call_btn',
                 class: "ui-btn-up-d",
                 "data-theme" => "d",
                 title: I18n.t('service_call.forms.work_status.reset.tooltip'),
                 rel:   'tooltip',
                 confirm: 'Are you sure?'
    %>
<% end %>

