<div class="span6">
<%= simple_form_for job.becomes(ServiceCall), html: { class: 'red-button' } do |f| %>
    <%= hidden_field_tag 'service_call[work_status_event]', 'un_accept' %>
    <%= f.submit job.class.human_work_status_event_name(:un_accept).titleize,
                 id:    'un_accept_service_call_btn',
                 class: 'btn btn-large btn-danger',
                 "data-placement" => "right",
                 title: I18n.t('service_call.forms.work_status.un_accept.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

  </div>