<%= simple_form_for job.becomes(ServiceCall), html: { class: style('service_call.forms.work_status.un_accept.form_classes') } do |f| %>
    <%= hidden_field_tag 'service_call[status_event]', 'un_accept' %>
    <%= f.submit job.class.human_status_event_name(:un_accept).titleize,
                 id:    'un_accept_service_call_btn',
                 class: style('service_call.forms.status.un_accept.button_classes'),
                 title: I18n.t('service_call.forms.status.un_accept.tooltip'),
                 rel:   'tooltip'
    %>
<% end %>

