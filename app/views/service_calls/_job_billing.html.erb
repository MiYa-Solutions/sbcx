<div class="well well_job_show well_job_history">
      <!--Billing Summary-->

      <div class="smart_label">
        <ul>
          <li><h1><%= I18n.t('general.general.billing_summary').html_safe %></h1></li>
          <li>
            <%= button_tag Invoice.model_name.human.pluralize, :class => 'btn btn-primary',
                           id: 'show_invoices_button', data: { toggle: "collapse", target: "#job_invoices" } unless ServiceCallsHelper::JobBillingFormsRenderer.new(@service_call, self).available_events.empty? %>
            <%= button_tag t('general.general.collect_payments'), :class => 'btn btn-primary',
                           id: 'new-collect-button', data: { toggle: "collapse", target: "#payment_collect" } unless ServiceCallsHelper::JobBillingFormsRenderer.new(@service_call, self).available_events.empty? %>

            <% unless @service_call.my_role == :subcon || @service_call.my_role == :broker %>
                <%= button_tag t('general.general.receipts'), :class => 'btn btn-primary',
                id: 'show_receipt_button', data: { toggle: "collapse", target: "#job_receipts" } unless ServiceCallsHelper::JobBillingFormsRenderer.new(@service_call, self).available_events.empty? %>
            <% end %>
            </li>
        </ul>
      </div>


      <div class="row-fluid job_billing_summary">
        <div class="span8 job_billing_summary_table">

          <!--General Summary-->
          <table class="table table-striped table-condensed table-sbcx customer_balance">
            <tbody>
            <% if @service_call.work_done? %>
            <tr>
              <td><strong> <%= t('general.general.my_profit').html_safe %></strong></td>
              <% if @service_call.try(:my_profit) < 0 %>
                  <td class="negative"><%= humanized_money_with_symbol @service_call.my_profit %></td>
              <% else %>
                  <% if @service_call.try(:my_profit) > 0 %>
                      <td class="positive"><%= humanized_money_with_symbol @service_call.my_profit %></td>
                  <% else %>
                      <td class="balanced"><%= humanized_money_with_symbol @service_call.my_profit %></td>
                  <% end %>

              <% end %>
              <td></td>

            </tr>
            <% end %>

                <!-- Balance-->
                <% case @service_call.my_role when :subcon %>
                    <%= render 'provider_balance' %>
                <% when :broker %>
                    <%= render 'provider_balance' %>
                    <%= render 'subcon_balance' %>
                <% else %>
                    <%= render 'customer_balance' %>
                    <%= render 'subcon_balance' unless @service_call.subcon_na? %>
                <% end %>


                </tbody>
              </table>
        </div>

        <div class="span4">
          <div class="collapse" id="payment_collect">
            <div class="collapse_frame collapse_collect">
            <div class="action_buttons_service_call collapse_body">
                <div class="row-fluid">
                  <a href="#" data-toggle="collapse" data-target="#payment_collect" class="close" id="bom_close">×</a>
                </div>

                  <div class="service_call_events">
                    <div class="row-fluid">
                        <%= billing_status_forms(@service_call) %>

                    </div>

                    <div class="row-fluid">
                      <div class="collapse" id="collect_job">

                        <%= simple_form_for @service_call.becomes(ServiceCall), html: { class: 'blue-button' } do |f| %>
                            <%= f.label :collector unless @service_call.available_payment_collectors.size == 1 %>
                            <%= collector_tag(@service_call) %>

                            <%= hidden_field_tag 'service_call[billing_status_event]', 'collect' %>

                            <%= f.input :payment_type, collection: payment_types, label: t('general.general.payment_type') %>
                            <%= f.input :payment_amount %>
                            <%= f.input :payment_notes %>
                            <%= f.submit I18n.t('general.general.submit'),
                                         id:          'collect_service_call_btn',
                                         class:       'btn btn-primary',
                                         "data-theme" => "b",
                                         title:       I18n.t('service_call.forms.billing_status.collection.tooltip'),
                                         rel:         'tooltip'
                            %>
                        <% end if @service_call.billing_status_events.include? :collect %>

                      </div>



                    </div>
                  </div>
            </div>
            </div>
         </div>
        <!--end span5-->
        </div>


      </div>






      <!-- Entries -->
  <%= render 'job_invoices' %>
  <%= render 'job_receipts' %>


  <% case @service_call.my_role when :subcon %>
          <%= render 'provider_entries' %>
      <% when :broker %>
          <%= render 'provider_entries' %>
          <%= render 'subcon_entries' %>
      <% else %>
          <%= render 'customer_entries' %>
          <%= render 'subcon_entries' unless @service_call.subcon_na? %>
      <% end %>

      <%= render 'job_boms' %>


  </div>