<% provide(:title, I18n.t('general.general.new_job').html_safe ) %>

<%= simple_form_for @service_call, as: :service_call, url: service_calls_url, :html => { :class => 'form-horizontal' } do |f| %>
    <%= render 'fields', f: f %>

    <!--<div class="form-actions">-->
      <%= f.submit I18n.t('general.general.create_job'), id: 'service_call_create_btn', "data-theme" => "b" %>
      <%= link_to I18n.t('general.general.back'), service_calls_path, "data-role" => "button", :class => "ui-btn-up-d" %>

    <!--</div>-->
<% end %>
