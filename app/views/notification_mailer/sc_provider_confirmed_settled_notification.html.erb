Dear <%= "#{@user.first_name} #{@user.last_name}" %>,

# TODO PENDING IMPLEMENTATION PROVIDER CONFIRMED SETTLEMENT

<%= I18n.t('notification_mailer.provider_confirmed_settled_service_call.content', provider: @service_call.provider.name, ref: @service_call.ref_id) %>

<%= link_to "#{ServiceCall.name.humanize} #{@service_call.ref_id}", @service_call.becomes(ServiceCall) %>