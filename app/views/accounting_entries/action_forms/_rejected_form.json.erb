<%= simple_form_for entry.becomes(AccountingEntry), remote: true, html: { 'data-type' => 'json', id: "adj_entry_rejected_form_#{entry.id}", class: 'adj_entry_reject_form entry_form ajax-form' } do |f| %>
    <%= f.hidden_field :status_event, value: 'rejected' %>
    <%= f.submit entry.class.human_status_event_name(:rejected).titleize, id: 'adjustment_entry_rejected_btn', class: 'customer_entry_small_btn btn btn-mini btn-danger ajax-btn' %>
<% end %>
