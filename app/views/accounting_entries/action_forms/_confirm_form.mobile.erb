<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'green-button' } do |f| %>
    <%= f.hidden_field :status_event, value: 'confirm' %>
    <%= f.submit entry.class.human_status_event_name(:confirm).titleize, id: "adjustment_entry_confirm_btn_#{entry.id}", class: "ui-btn-up-b ui-btn-hidden", "data-theme" => "b"  %>
<% end %>


