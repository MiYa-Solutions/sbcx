<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'red-button' } do |f| %>
    <%= f.hidden_field :status_event, value: 'cancel' %>
    <%= f.submit entry.class.human_status_event_name(:cancel).titleize, id: 'adjustment_entry_cancel_btn', class: "ui-btn-up-b ui-btn-hidden", "data-theme" => "r" %>
<% end %>
