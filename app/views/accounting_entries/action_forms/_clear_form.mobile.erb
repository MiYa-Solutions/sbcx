<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'adj_entry_clear_form' } do |f| %>
    <%= f.hidden_field :status_event, value: 'clear' %>
    <%= f.submit entry.class.human_status_event_name(:clear).titleize, id: 'entry_clear_btn', class: "ui-btn-up-b ui-btn-hidden", "data-theme" => "b" %>
<% end %>
