<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'adj_entry_reject_form' } do |f| %>
    <%= f.hidden_field :status_event, value: 'reject' %>
    <%= f.submit entry.class.human_status_event_name(:reject).titleize, id: 'adjustment_entry_reject_btn' %>
<% end %>
