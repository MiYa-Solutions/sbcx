<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'adj_entry_confirmed_form' } do |f| %>
    <%= f.hidden_field :status_event, value: 'confirmed' %>
    <%= f.submit entry.class.human_status_event_name(:confirmed).titleize, id: 'adjustment_entry_confirmed_btn', class: css_class %>
<% end %>
