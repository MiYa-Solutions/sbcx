<%= simple_form_for entry.becomes(AccountingEntry), html: { class: 'adj_entry_disputed_form' } do |f| %>
    <%= f.hidden_field :status_event, value: 'disputed' %>
    <%= f.submit entry.class.human_status_event_name(:disputed).titleize, id: 'adjustment_entry_disputed_btn', class: css_class %>
<% end %>
