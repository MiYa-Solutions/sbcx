<% provide(:title, "Edit Organization") %>
<h1>Update Your Organization Profile</h1>

<div class="row">
  <div class="span6 offset3">
    <%= simple_form_for(@organization) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>

  </div>
</div>