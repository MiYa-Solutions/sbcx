<ul class="unstyled">
  <% @users.each do |user| %>
      <li>
        <%= link_to user.email, my_user_path(user) %>
        <% user.roles.each do |role| %>
            (<%= role.name %>)

        <% end %>
      </li>
  <% end %>

</ul>