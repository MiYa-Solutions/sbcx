<div id="state_code_wrapper">
  <% parent_region ||= params[:parent_region] %>
  <% country = Carmen::Country.coded(parent_region) %>

  <% if country.nil? %>
      <%= subregion_select(f.object, :state, 'US') %>
  <% elsif country.subregions? %>
      <%= subregion_select(f.object, :state, parent_region) %>
  <% else %>
      <%= text_field(f.object, :state) %>
  <% end %>
</div>