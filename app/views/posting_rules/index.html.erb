<h1>Listing posting_rules</h1>

<table>
  <tr>
    <th>Agreement</th>
    <th>Type</th>
    <th>Rate</th>
    <th>Rate type</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @posting_rules.each do |posting_rule| %>
      <tr>
        <td><%= posting_rule.agreement_id %></td>
        <td><%= posting_rule.type %></td>
        <td><%= posting_rule.rate %></td>
        <td><%= posting_rule.rate_type %></td>
        <td><%= link_to 'Show', agreement_posting_rule_path(posting_rule.agreement, posting_rule) %></td>
        <td><%= link_to 'Edit', edit_agreement_posting_rule_path(posting_rule.agreement, posting_rule) %></td>
        <td><%= link_to 'Destroy', agreement_posting_rule_path(posting_rule.agreement, posting_rule), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <td><%= link_to 'New Posting rule', new_agreement_posting_rule_path(posting_rule.agreement) %></td>
      </tr>
  <% end %>
</table>

<br/>


