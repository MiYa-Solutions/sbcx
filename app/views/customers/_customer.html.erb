<h1> Organization Attributes</h1>
<% for attribute in @customer.attributes.keys  %>
    <b><%= attribute.humanize %></b> <%= @customer.attributes[attribute].to_s %><br />
<% end %>

  </p>
  <div class="form-actions">
    <%= link_to t('.back', :default => t("helpers.links.back")),
                organization_customer_path, :class => 'btn'  %>
    <%= link_to t('.edit', :default => t("helpers.links.edit")),
                edit_organization_customer_path(@organization), :class => 'btn' %>
    <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                organization_customer_path(@organization),
                :method => 'delete',
                :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
                :class => 'btn btn-danger' %>
  </div>

