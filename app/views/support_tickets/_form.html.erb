<%= simple_form_for(@support_ticket, :html => {:class => 'form-horizontal' } ) do |f| %>
<%= f.error_notification %>

      <%= f.input :subject %>

    <div class="newJob_note_area">
      <%= f.input :description %>
    </div>

    <div class="form-actions">
      <%= link_to 'Back', support_tickets_path, class: "btn btn-medium" %>
      <%= f.button :submit, class: "btn btn-medium btn-primary" %>
    </div>
<% end %>



