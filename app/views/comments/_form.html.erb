<%= simple_form_for comment, remote: true do |f| %>
    <%= f.input :body, :input_html => { :rows => "2" }, :label => false, :placeholder => "Enter your comment and click the button below." %>
    <%= f.input :commentable_id, :as => :hidden, :value => comment.commentable_id %>
    <%= f.input :commentable_type, :as => :hidden, :value => comment.commentable_type %>
    <%= f.button :submit, :class => "btn btn-medium btn-primary", :disable_with => "Submitting…" %>
<% end %>