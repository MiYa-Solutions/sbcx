<%= render 'shared/error_messages', object: f.object %>

<%= f.input :name %>

<%= f.input :company %>

<%= f.input :phone %>

<%= f.input :website, as: :url%>

<%= f.input :address1 %>

<%= f.input :address2 %>

<%= f.country_select :country, { priority: %w[US] } %>

<%= render  'region_select/subregion_select' %>
